name: Build & push OIDC image

on:
  push:
    branches: [oidc]
  schedule:
    - cron: "0 5 * * 1"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE: ${{ github.repository_owner }}/invoiceninja-oidc
  UPSTREAM: invoiceninja/invoiceninja

jobs:
  sync-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: oidc
          fetch-depth: 0

      - name: Sync with upstream
        if: github.event_name == 'schedule'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/${{ env.UPSTREAM }}.git || true
          LATEST_TAG=$(git ls-remote --tags upstream 'refs/tags/v5.*' | sort -t/ -k3 -V | tail -1 | sed 's/.*refs\/tags\///')
          git fetch upstream "$LATEST_TAG"
          if git rebase "FETCH_HEAD"; then
            git push origin oidc --force-with-lease
          else
            git rebase --abort
            echo "::warning::Rebase failed — manual intervention needed"
          fi

      - name: Detect version
        id: version
        run: |
          VERSION=$(grep -oP '"version":\s*"\K[^"]+' composer.json | head -1)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.oidc
          target: invoiceninja-oidc
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE }}:${{ steps.version.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE }}:latest
